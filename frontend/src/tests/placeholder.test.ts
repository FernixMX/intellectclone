/**
 * Placeholder: los tests reales se agregan en Fase E.
 * Vitest debe pasar con 0 tests para el punto de control de Fase A.
 */
describe("placeholder", () => {
  it("placeholder pasa", () => {
    expect(true).toBe(true);
  });
});
